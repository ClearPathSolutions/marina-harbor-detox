# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# heavy local archive of the old WordPress site (kept locally, not deployed).
# Remove these two lines if you want the full media library + raw HTML committed too.
/archive/media/
/archive/raw-html/

# Throwaway analysis/screenshot scripts run against a local server.
# These got committed once by an over-broad `git add -A`; keep them out.
/audit-*.mjs
/design-*.mjs
/probe*.mjs
/shot.mjs
/crop.mjs

# Playwright MCP scratch output (browser snapshots / console logs)
.playwright-mcp/
